<div id="swfupload-control">
	<span id="swfupload-button"></span>
</div>

<ul id="swfupload-playlist" class="playlist">
  <% @posts.each do |post| %>
    <li id="" class=""><%= render post %></li>
  <% end if @posts %>
</ul>

<%= render 'player' %>

<ul class="users">
  <% @users.each do |user| %>
    <li><b><%= link_to h(user), user %></b></li>
  <% end if @users %>
</ul>

<% content_for :head do %>
  <%= render 'uploader' %>
<% end %>
