<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html; charset=UTF-8" />
  <title>KZAK!</title>
  <%= stylesheet_link_tag 'reset', 'application', 'jquery.soundmanager', :cache => true %>
  <%= javascript_include_tag 'application', 'jquery', 'jquery.livequery', 'soundmanager', 'jquery.soundmanager', 'swfupload', 'jquery.swfupload', :cache => true  %>
  <%= yield :head %>
  <script type="text/javascript" charset="utf-8">
    $(document).ready(function(){
      $.playable('flash/',{pauseSkip : false, useUi : true});
      $('ul.playlist').playable();
      // $.playable('flash/',{pauseSkip : false, useUi : true});
      // $('ul.playlist').livequery(function(){ $(this).playable(); });
    });
  </script>
</head>
<body>
  <%= image_tag('bplayer2.gif', :class => 'bplayer2') %>
  <h1><%= link_to 'KZAK!', root_path %></h1>
  <%= yield %>  
</body>
</html>
